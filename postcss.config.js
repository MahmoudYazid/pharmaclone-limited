/* eslint-disable prettier/prettier */
module.exports = {
  plugins: [
    require('tailwindcss'),
    require('postcss-react-native')({
      // Add any custom configuration options here
    }),
  ],
};
